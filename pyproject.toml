[project]
name = "keystone"
version = "0.1.0"
description = "The Delegation Layer — bespoke micro-automations from natural language"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "agent-sandbox>=0.0.21",
    "claude-agent-sdk",
]
